# SquashFS 파서 및 추출기 (SquashFS_Parser)

이 스크립트는 **SquashFS 버전 4** 파일 시스템 이미지를 파싱하고, 이미지에 포함된 파일과 디렉터리 구조를 지정된 폴더로 추출하는 파이썬 도구입니다.

## 🚀 주요 기능

* **SquashFS v4 지원**: SquashFS 버전 4.x로 생성된 이미지를 분석합니다.
* **다양한 압축 방식 지원**:
    * 기본 지원: **gzip**, **xz**, **lzma**
    * 선택적 지원: **lz4**, **zstd**, **lzo** (라이브러리 설치 필요)
* **메타데이터 보존**:
    * 파일 및 디렉터리 권한 (`chmod`)
    * 소유자/그룹 정보 (`chown`) (UID/GID 테이블 파싱)
    * 확장 속성 (**xattr**)
* **파일 시스템 재귀 추출**: 이미지 내의 모든 파일, 디렉터리, 심볼릭 링크(symlink)를 재귀적으로 탐색하여 로컬 디스크에 동일한 구조로 생성합니다.
* **추출 결과 요약**: 작업 완료 후, 추출된 파일, 디렉터리 수, 총 용량, 압축 방식 등 상세한 요약 정보를 콘솔에 출력합니다.

---

## 🔧 설치 및 준비

스크립트 실행을 위해 필요한 외부 라이브러리를 설치합니다. `requirements.txt` 파일에 다음 내용을 저장하고 설치하세요.

**`requirements.txt`**
```txt
lz4==4.4.4
python-lzo==1.15
zstandard==0.25.0
'''

### 설치 명령어
pip install -r requirements.txt

### 실행 방법 (결과)
** 기본 사용법
python squashFS_parser.py [이미지 파일]

### 실행 결과
압축 방식
블록 크기
총 Inode 수 -> 디렉터리, 일반 파일, 심볼릭 링크, 기타
총 기록된 바이트 수
평균 파일 크기
Fragment 엔트리 수
사용자 고유 tail fragment 수
최대 디렉터리 깊이

** 옵션
-o : 출력 디렉터리 지정
예시 :python squashFS_parser.py firmware.img -o ./extracted_firmware

--no-meta : 메타데이터 적용 안 함
python squashFS_parser.py firmware.img --no-meta